@model IEnumerable<Infraestructure.Models.CUADRACICLO>

@{
    ViewBag.Title = "Mantenimiento Cuadras";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div style="padding:5%">
    <h1>@ViewBag.titulo</h1>

    <p>
        @Html.ActionLink("Nuevo", "Create")
    </p>
    <table class="table table-hover" style="text-align:center">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MARCA.DESCRIPCION)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TARIFA)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.INVENTARIO)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr class="table-secondary">
                <td>
                    @Html.DisplayFor(modelItem => item.MARCA.DESCRIPCION)
                </td>
                <td>
                    ¢@Html.DisplayFor(modelItem => item.TARIFA)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.INVENTARIO)
                </td>
                <td>
                    @Html.ActionLink("Editar", "Edit", new { id = item.ID_CUADRACICLO }) |
                    @Html.ActionLink("Detalle", "Details", new { id = item.ID_CUADRACICLO }) |
                    @Html.ActionLink("Eliminar", "Delete", new { id = item.ID_CUADRACICLO })
                </td>
            </tr>
        }

    </table>
</div>